// last 60 milliseconds, adjust this value to tune the animation
const LAYOUT_ANIMATION_DURATION_MS = 60

enum PriorityModeOptions {
  Universal = 'Universal',
  Vote = 'Vote',
}

export { LAYOUT_ANIMATION_DURATION_MS, PriorityModeOptions }
